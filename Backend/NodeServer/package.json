{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "The Server aggregates all Signals from the riddles and presents them to the Frontend.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "onoff": "^6.0.3"
  }
}
